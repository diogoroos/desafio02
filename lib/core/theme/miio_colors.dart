import 'package:flutter/material.dart';

/// Estilos de cores seguidos do figma do desafio
class MiioColors {
  /// cor de fundo principal
  static const Color fundoPrincipal = Color(0xFFFBFBFB);

  /// cor de fundo detalhe
  static const Color fundoDetalhe = Color(0xFFFFFFFF);

  /// cor de texto de destaque de botao
  static const Color textoDestaque = Color(0xFF222222);

  /// cor de fundo botoes sobre imagem detalhe
  static Color fundoBotoesDetalhe = const Color(0xFF222222).withAlpha(80);

  /// cor de conteúdo de destaque
  static const Color conteudoDestaque = Color(0xFFA9A9A9);

  /// cor de destaque do card do detalhe
  static const Color destaqueDetalhe = Color(0xFF000000);

  /// texto secundário
  static const Color textoClaro = Color(0xFFDADADA);

  /// cor do componente semelhante a divisor do topo da página detalhe (slider)
  static const Color slider = Color(0xFFE5E5E5);

  /// cor de borda e botao secundário
  static const Color secundario = Color(0xFFE6E6E6);

  /// cor do card da página de detalhe
  static const Color cardDetalhe = Color(0xFFF9FBFC);

  /// cor de botoes sobre a imagem no detalhe e scaffold
  static const Color branco = Color(0xFFFFFFFF);

  /// cor estrela selecionada
  static const Color estrelaSelecionada = Color(0xFF66C2E9);

  /// cor card comentario
  static Color cardComentario = const Color(0xFFC4C4C4).withAlpha(10);

  /// cor do botao featured
  static const Color botaoFeatured = Color(0xFF92D55B);

  /// cor do botao filtro selecionado
  static const Color botaoFiltroSelecionado = Color(0xFFA1D2E5);

  /// cor do botao filtro apagado
  static const Color botaoFiltroApagado = Color(0xFFF0F9FC);
}
